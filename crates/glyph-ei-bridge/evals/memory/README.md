# Eval Memory

The first golden trace should prove that EI semantic capsules can force a Glyph controller to clarify before generating when a request contains a pragmatic contradiction.

Regression rule: any future bridge change must keep `sc_sarcastic_sincere_apology` passing and keep the naive baseline failing.
