{
  "name": "glyph",
  "version": "0.1.0",
  "description": "Glyph is a compact executable control language for small models operating high-level harnesses.",
  "type": "module",
  "bin": {
    "glyph": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "glyph": "tsx src/cli/index.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.9"
  }
}
